---
- hosts: all

  vars_files:
    - default.config.yml

  roles:
    - role: mariuskimmina.dotfiles
      when: configure_dotfiles
      tags: ['dotfiles']
    - role: mariuskimmina.dev.go
      tags: ['go']
